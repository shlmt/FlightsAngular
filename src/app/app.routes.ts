import { Routes } from '@angular/router'
import { FlightsPageComponent } from './features/flights/flights-page/flights-page.component'

export const routes: Routes = [
  { path: 'flights', component: FlightsPageComponent }
]
